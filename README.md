# 🧠 ToguzZero: Deep Reinforcement Learning for Toguz Kumalak

**ToguzZero** — это современный искусственный интеллект для казахской национальной игры Тогызкумалак, разработанный с использованием методов глубокого обучения с подкреплением (Deep Reinforcement Learning), вдохновленных алгоритмом AlphaZero.

Проект создан для демонстрации возможностей ИИ в стратегических играх с полной информацией.

## 🚀 Особенности

- **Архитектура:** Нейросеть ResNet (5 Residual Blocks) для оценки позиции.
- **Поиск:** Monte Carlo Tree Search (MCTS) для глубокого тактического расчета.
- **Обучение:** Модель обучена методом Self-Play (игра сама с собой), перейдя от случайной игры к уровню КМС/Мастера спорта.
- **Результаты:** В тестовых матчах модель одержала победу над версией Supervised Learning со счетом 20-0.

## 📂 Структура проекта

- `src/model.py` — Архитектура нейросети (PyTorch).
- `src/mcts.py` — Реализация алгоритма поиска MCTS.
- `src/game.py` — Логика и правила игры Тогызкумалак.
- `models/` — Папка с обученными весами модели.
- `main.py` — Скрипт для игры Человек против ИИ.

## 🛠 Установка и Запуск

1. **Клонируйте репозиторий:**

    ```bash
    git clone [https://github.com/KaiDev07/9QQ.git](https://github.com/KaiDev07/9QQ.git)
    cd 9QQ
    ```

2. **Установите зависимости:**
   Для работы требуется Python 3.8+ и PyTorch.
    ```bash
    pip install -r requirements.txt
    ```
3. **Запустите игру:**
    ```bash
    python main.py
    ```
    Вы сможете выбрать цвет и сыграть против обученной модели.

## 🏋️‍♂️ Обучение модели (Training)

Если вы хотите продолжить обучение модели с подкреплением:

```bash
python train.py
```

Скрипт начнет генерировать партии (Self-Play) и улучшать веса модели, сохраняя результат в models/toguz_best.pth.

**🧠 Технические детали**
В основе лежит итеративный процесс улучшения:

1. Neural Network предсказывает вероятности ходов ($\pi$) и оценку позиции ($v$).
2. MCTS использует эти предсказания для построения дерева вариантов.
3. Данные из MCTS используются для дообучения сети, замыкая цикл улучшения.

**👤 Авторы**

Ақай Қайназар

Марат Ахмед

Жумамашев Алихан
